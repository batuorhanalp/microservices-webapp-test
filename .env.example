# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env files with real secrets!

# ==========================================
# DEPLOYMENT CONFIGURATION
# ==========================================
APP_NAME=webapp-prod
ENVIRONMENT=dev
CLOUD_PROVIDER=azure
AZURE_REGION=East US

# ==========================================
# AZURE CONFIGURATION
# ==========================================
# Azure CLI authentication (use: az login)
# AZURE_SUBSCRIPTION_ID=your-subscription-id-here
# AZURE_TENANT_ID=your-tenant-id-here

# ==========================================
# TERRAFORM CONFIGURATION  
# ==========================================
# Terraform will use Azure CLI authentication by default
# No need to set credentials in environment variables

# Backend configuration (optional - for remote state)
# TF_VAR_backend_storage_account=your-terraform-state-storage
# TF_VAR_backend_container=terraform-state
# TF_VAR_backend_key=webapp-prod/terraform.tfstate

# ==========================================
# KUBERNETES CONFIGURATION
# ==========================================
# Kubernetes context (set automatically after deployment)
# KUBECONFIG=~/.kube/config

# ==========================================
# APPLICATION SECRETS (DO NOT SET HERE!)
# ==========================================
# These are managed by Azure Key Vault:
#
# Database secrets:
# - DATABASE_PASSWORD (auto-generated in Key Vault)
# - DATABASE_CONNECTION_STRING (auto-generated in Key Vault)
#
# Redis secrets:
# - REDIS_PASSWORD (managed by Azure Cache for Redis)
# - REDIS_CONNECTION_STRING (auto-generated in Key Vault)
#
# Application secrets:
# - JWT_SECRET (auto-generated in Key Vault)
# - ENCRYPTION_KEY (auto-generated in Key Vault)
# - GRAFANA_ADMIN_PASSWORD (auto-generated in Key Vault)
#
# Azure service secrets:
# - STORAGE_ACCOUNT_KEY (managed by Azure Storage)
# - CONTAINER_REGISTRY_PASSWORD (managed by Azure Container Registry)
# - EVENT_HUB_CONNECTION_STRING (managed by Azure Event Hub)
# - APP_INSIGHTS_CONNECTION_STRING (managed by Azure Application Insights)

# ==========================================
# LOCAL DEVELOPMENT (Optional)
# ==========================================
# Only use these for local development with mock/test data
# LOCAL_DB_HOST=localhost
# LOCAL_DB_PORT=5432
# LOCAL_DB_NAME=webapp_dev
# LOCAL_DB_USER=dev_user
# LOCAL_DB_PASSWORD=dev_password_change_me

# LOCAL_REDIS_HOST=localhost
# LOCAL_REDIS_PORT=6379

# ==========================================
# DEVELOPMENT TOOLS
# ==========================================
# Docker Compose settings
COMPOSE_PROJECT_NAME=webapp-prod-dev

# Logging level for development
LOG_LEVEL=debug

# Development server ports
API_PORT=3000
FRONTEND_PORT=3001

# ==========================================
# SECURITY NOTES
# ==========================================
#
# 🔐 NEVER commit .env files with real values!
# 🔐 Use Azure Key Vault for all production secrets
# 🔐 Use managed identities for Azure authentication
# 🔐 Rotate secrets regularly
# 🔐 Use different secrets for each environment
#
# Production secrets are automatically:
# ✅ Generated with cryptographically secure methods
# ✅ Stored in Azure Key Vault
# ✅ Accessed via managed identities
# ✅ Injected into applications at runtime
# ✅ Never stored in code or configuration files
#
# ==========================================
