apiVersion: v1
kind: Namespace
metadata:
  name: webapp-staging
  labels:
    app: webapp-production
    environment: staging
    managed-by: kustomize
  annotations:
    description: "Staging web application namespace"
    contact: "devops-team"
